# 飞书/Lark 多维表格同步配置指南

要实现“一键初始化”和“云端同步”，您需要完成以下 3 个步骤获取关键参数。

## 第一步：获取 App ID 和 App Secret
1. 电脑浏览器打开 [飞书开放平台](https://open.feishu.cn/app?lang=zh-CN)。
2. 点击右上角 **“创建应用”** -> 选择 **“企业自建应用”**。
3. 输入名称（例如：`鱼跃CRM同步`），点击创建。
4. 在左侧菜单点击 **“凭证与基础信息”**。
5. 这里您能看到 **App ID** 和 **App Secret**。
   > 👉 **请复制这两个值到软件设置中。**

---

## 第二步：开通权限并发布 (最关键！)
为了让软件能自动创建表格，必须开通权限。

1. 在应用详情页左侧，点击 **“开发配置” -> “权限管理”**。
2. 在搜索框输入 `多维表格`，勾选以下所有关键权限：
   - 建议搜索 `bitable` 把相关的都勾上（特别是 **查看、编辑、管理多维表格**）。
   - **核心权限**：`bitable:app:read`, `bitable:app:run`, `bitable:app:open:manage`
3. 点击 **“批量开通”**。
4. **⚠️ 重要：** 点击左侧 **“应用发布” -> “版本管理与发布”**。
5. 点击 **“创建版本”** -> 输入版本号（如 1.0.0） -> **“申请发布”**。

---

## 第三步：获取 App Token (Base Token)
1. 打开您的飞书电脑版或网页版。
2. **新建一个多维表格** (或者使用现有的)。
3. 看一下这个表格的 **分享链接** 或 **浏览器地址栏**。
   - 链接格式通常是：`https://example.feishu.cn/base/bascnXXXXXXXXXXXXXX?table=tblYYYYYY`
4. `base/` 后面，`?` 前面的那串字符（以 `bas` 开头）就是 **App Token**。
   - 例如：`bascnHk69f8a0s8d7f6g5h4j3k2`
   > 👉 **请复制这个 Token 到软件设置中。**

---

## 第四步：添加机器人到表格 (最后一步)
软件是通过这个“机器人”来读写数据的，所以必须把它拉进群（表格）里。

1. 打开刚才那个多维表格。
2. 点击右上角的 **“...” (更多菜单)**。
3. 选择 **“添加文档应用”** (Add App)。
4. 搜索您刚才创建的应用名字（例如 `鱼跃CRM同步`）。
5. **一定要点击“添加”，并获得“管理者”权限！**
   - ⚠️ 注意：仅仅添加是不够的，如果默认是“阅读者”或“编辑者”，可能无法创建数据表。**必须确保它是“管理者” (Manager)**。

✅ **完成！** 现在回到软件设置页：
1. 填入 ID, Secret, Token。
2. 点击 **“一键初始化表格”**。
