# 鱼跃CRM 轻量化方案 (Tauri) - 实施计划

该方案通过从 Electron 迁移到 Tauri，实现了极致的体积优化和本地数据隔离。

## 用户需求要点
- **数据隔离**：通过本地独立数据库确保数据安全。
- **体积优化**：使用 Tauri 将安装包缩减至 11MB 左右。
- **品牌定制**：羽毛图标与“鱼跃CRM”品牌全路径集成。

## [已实施] 技术架构变更
- **核心框架**: Tauri (v2)
- **数据库**: Rust 驱动的 SQLite (`financial.db`)。
- **安全认证**: 使用高性能 Rust 命令处理加密逻辑。

## [已实施] 关键修复与增强

### 1. 导出功能修复 (Export Fixes)
- **DataContext**: 修复了 `csvContent` 未定义错误，并集成了集中式 `refreshData` 逻辑。
- **Dashboard**: 修正了备份导出时的变量作用域命名冲突。
- **InvoiceModal**: 升级为 Tauri 原生文件保存、图片及 PDF 导出。

### 2. 安全性增强 (Security Upgrade)
- **原生确认对话框**: 将所有 `window.confirm` 手动替换为 Tauri `ask` 插件 API。
- **受保护操作**: 涵盖首页重置、客户删除、交易删除及分类删除，均有系统级二次确认。

### 3. UI 视觉优化 (UI Refinement)
- **动态曲线图表**: 将首页“本周营收分布”由阶梯图切换为平滑曲线图 (`monotone`)，提升了财务数据的展示美感。
- **响应式刷新**: 优化了客户页面的强制刷新逻辑，确保数据实时同步。

## 验证计划 (最终状态)
- [x] 验证安装包体积约 11MB。
- [x] 验证所有导出按钮均能弹出原生保存对话框且数据正确。
- [x] 验证所有删除操作均弹出 Windows 原生确认窗口。
- [x] 验证首页营收分布显示为平滑绿色曲线。
